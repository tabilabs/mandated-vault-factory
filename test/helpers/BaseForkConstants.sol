// SPDX-License-Identifier: CC0-1.0
pragma solidity ^0.8.24;

// --- Fork configuration ---
uint256 constant BASE_CHAIN_ID = 8453;
uint256 constant BASE_FORK_BLOCK = 0;

// --- Tokens (placeholders; to be finalized by official sources) ---
address constant BASE_USDC = address(0);
address constant BASE_WETH = address(0);

// --- Protocol anchors (placeholders; to be finalized by official sources) ---
address constant BASE_AAVE_POOL = address(0);
address constant BASE_MORPHO = address(0);
address constant BASE_UNISWAP_V3_ROUTER = address(0);
address constant BASE_AERODROME_ROUTER = address(0);
address constant BASE_COMPOUND_COMET = address(0);
